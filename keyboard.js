// const buttons = document.querySelectorAll('.btn');
// const textarea = document.querySelector('textarea');

// buttons.forEach(btn >= {
//     btn.addEventListener('click',)
// })

function keys(){
    //need a way to detect whichever letter was clicked in order to be able to compare it to the generated word
    var keyPressed = document.getElementById('letter').innerHTML; //store the letter shown on the button in a variable
    var box = document.getElementById('lettersClicked').innerHTML
    box = letterBox + keyPressed; //not working
}